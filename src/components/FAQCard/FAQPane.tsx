import React, { ReactElement } from "react";

interface FAQPaneProps {}

const FAQPane: React.FC<FAQPaneProps> = (): ReactElement => {
  return <div className="faq-pane">pane</div>;
};

export default FAQPane;
